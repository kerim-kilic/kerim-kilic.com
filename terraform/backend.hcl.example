# Copy to backend.hcl (gitignored) and fill in. Used by every environment:
#   terraform init -backend-config=../../backend.hcl
bucket = "YOUR-TERRAFORM-STATE-BUCKET"
region = "eu-central-1"
# profile = "your-aws-profile"   # optional, if you use named profiles / SSO
